services:
  backend:
    build: ./backend
    image: likian/quiz-backend:latest
    ports:
      - "5000:5000"

  frontend:
    build: ./frontend
    image: likian/quiz-frontend:latest
    ports:
      - "8080:80"
    depends_on:
      - backend
